# VKD3D Gate C Evidence - Maleoon 910 - 2026-08-04

## Scope

- Branch: `feature/vkd3d-capability-probe`
- Device: `62T0225B10005882` (Maleoon 910)
- Main experiment harness commit: `aaba906`
- Wine bootstrap commit: `a3f725d02d7`
- VirGL/Venus Gate C commit: `152b762c`
- HAP SHA-256: `e3cd92ba2bc0cfdd5004e9340ee09ce17caa529f3cf85f046be4960959747a6e`
- Embedded `wine-data.zip` SHA-256: `8564cc28e16c3187130cff863b104261428b92cc5c0833c036bbf05e521fb50f`
- VKD3D version: `v2.6-65f47dc-local-query-meta-disabled`
- VKD3D build ID: `0x65f47dc2a0a9703`
- Experiment ID: `vkd3d-500k-65f47dc-coherent`
- `d3d12.dll` SHA-256: `024a601a3e57080a96d2e35d5be8e075923920941fdf0ba402b1a3981e285b2d`
- Microtest SHA-256: `d25792283f899261de7a762216f30b89ecd5083fed97b827f692a27f1c3b1e33`

Every qualified run used `force-stop -> uninstall -> install -> wakeup -> clear logs -> launch`.
Before every install, the HAP timestamp and hash, embedded ZIP hash, Guest x86-64
EGL, and Host AArch64 `libentry.so` were rechecked. The payload was downloaded
through an HDC reverse loopback port and re-hashed by the application before
being copied into a new `.wine-smoke` prefix.

## Qualified Runs

| Run | Clean wineboot | Explicit heap | Checkpoint | Result | Process exit |
| --- | --- | --- | --- | --- | --- |
| `gate-c-valid-1-20260804` | PASS (24 s) | 500,000 | `RUNNING/startup` -> `PASS_PENDING_TEARDOWN/complete` | PASS | 0 |
| `gate-c-valid-2-20260804` | PASS (25 s) | 500,000 | `RUNNING/startup` -> `PASS_PENDING_TEARDOWN/complete` | PASS | 0 |
| `gate-c-valid-3-20260804` | PASS (25 s) | 500,000 | `RUNNING/startup` -> `PASS_PENDING_TEARDOWN/complete` | PASS | 0 |

All three result records are byte-for-byte equivalent:

```json
{"status":"PASS","stage":"complete","message":"500K descriptor heap and D3D12 copy/readback passed"}
```

Wine loader tracing records the selected module as:

```text
Loaded C:\smoke\experiments\vkd3d-500k-65f47dc-coherent\d3d12.dll: native
```

The microtest source contains no `vkDeviceWaitIdle`, `Sleep`, `sleep`, or global
flush compensation. It exercises device, queue, fence, upload/default/readback,
byte-for-byte validation, descriptor writes, and a 500,000-entry shader-visible
resource heap.

## Status

All three required consecutive, independently reinstalled runs passed with the
same result and exit status. The 500K Gate C device/copy/readback microtest is
complete on this 910 device for the recorded HAP, payload, driver, and runtime
hashes. This does not enable D3D12 by default and does not yet qualify real game
testing; descriptor/BDA/multi-queue/BrokerPresent work and the 1,000-frame
physical-display gate remain separate follow-up gates.

A preceding diagnostic run also returned PASS using the microtest's default
500,000 count, but its HDC Want arguments were empty and no checkpoint was
written. It is intentionally excluded from the qualified sequence.
